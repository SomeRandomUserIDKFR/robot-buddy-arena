/**
 * Entry used by vendor-model to produce vendor/analyzer-runtime.js
 * (browser ESM bundle with onnxruntime inlined; WASM still loaded locally).
 */
export { env, pipeline } from "@huggingface/transformers";
