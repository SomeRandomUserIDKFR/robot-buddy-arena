# Runtime attribution

- **@huggingface/transformers** 3.7.2 — Apache-2.0 (bundled into `analyzer-runtime.js`)
- **onnxruntime-web** — MIT (WASM under `ort-wasm/`)
- Gameplay never downloads models or runtimes from the network.

Reproduce assets with `npm run vendor-model`.
